let numberType:number;
let strType:string;
let booleanType:boolean;
let arrayType:any[];
let initializeArray:number[]=[1,2,3,4];

enum Weekday {
    MONDAY, 
    TUESDAY, 
    WEDNESDAY, 
    THURSDAY, 
    FRIDAY
}

let weekday:Weekday;